import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuandv.xsmb',
  appName: 'XSMB Miền Bắc',
  webDir: 'build',
  ios: {
    contentInset: 'automatic',
    backgroundColor: '#b91c1c',
  },
};

export default config;
